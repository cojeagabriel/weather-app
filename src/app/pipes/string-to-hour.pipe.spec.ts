import { StringToHourPipe } from './string-to-hour.pipe';

describe('StringToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
