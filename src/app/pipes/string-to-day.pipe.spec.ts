import { StringToDayPipe } from './string-to-day.pipe';

describe('StringToDayPipe', () => {
  it('create an instance', () => {
    const pipe = new StringToDayPipe();
    expect(pipe).toBeTruthy();
  });
});
